# CLAUDE.md

## 这是什么仓库

这是配合个人技术博客的教程视频生产资料库，  
存放录制、配音、发布前用到的脚本、模板和工作流说明，  
不存放原始录像或剪辑工程文件。

## 为什么是独立仓库

博客仓库是公开的，这里存放的内容大多是发布前的草稿，  
比如还没定稿的选题、脚本要点、标题候选，  
不适合和公开博客混在一起，所以单独建了这个私有仓库。

## 和博客仓库的关系

视频和博客文章是两个独立、松耦合的产出物，  
不是每篇博客都会配视频，也不是每期视频都改编自博客，  
所以两者不物理嵌套在一起。  
如果某期视频是基于某篇博文改编的，  
脚本里会记录博文链接作为引用，而不是把两类内容放进同一目录。

## 目录结构与约定

- `guides/` 是参考资料，说明脚本格式和录制工作流的设计理由，  
  不对应任何一期具体视频。
- `templates/` 是可复用模板，本身不对应任何一期具体视频。
  - `script.md` / `script.decision.md`：脚本模板及设计理由。
  - `metadata.md` / `metadata.decision.md`：发布信息模板及设计理由。
- `episodes/` 下每期视频对应一个 `episode-NN/` 子目录，  
  正式录制时创建，从 `templates/` 复制  
  `script.md`、`metadata.md` 进去填写，具体步骤见  
  `episodes/README.md`。
- 原始录像、剪辑工程文件不进这个仓库，  
  体积太大也没必要版本控制，脚本里需要引用时记录路径或云盘链接。

## 脚本格式与工作流

- 脚本格式的取舍说明见 `guides/script-format.md`。
- 完整录制工作流（先剪画面、后配音）见  
  `guides/tutorial-video-workflow.md`。

## 来源

这套结构最初是在 `rsdwd-document` 仓库的 `promotion/output/` 下  
设计出来的，本仓库是该设计从分析阶段搬到实际使用阶段的产物，  
`rsdwd-document` 里保留了一份副本，记录设计过程本身。
